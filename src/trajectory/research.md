Advanced Trajectory Optimization and Motion Profiling for Skid-Steer Micromouse Systems
=======================================================================================

Introduction to Micromouse Trajectory Dynamics
----------------------------------------------

The navigation of autonomous robotic agents within highly constrained environments, particularly in the competitive
context of Micromouse labyrinths, represents a profound challenge bridging applied control theory, non-holonomic
kinematics, and real-time trajectory optimization. The transition from rudimentary, grid-based topological navigation to
fluid, high-speed, "human-like" motion requires discarding discrete orthogonal movement paradigms in favor of
continuous-curvature spatial planning and jerk-limited dynamic velocity profiling.

In baseline systems, robotic trajectories are generated by simply linking cell centers via orthogonal straight lines and
terminating at each cell boundary to execute a turn-in-place maneuver. The provided initial software architecture relies
on a fundamental `Trajectory::from_cell_path` implementation that parses a raw orthogonal array and generates
`Segment::Straight` and `Segment::TurnInPlace` enums. While this approach guarantees structural safety and vastly
simplifies localization by keeping the robot parallel to the maze walls, it is mathematically and mechanically
suboptimal. The necessity to decelerate to a zero-velocity state before initiating an angular rotation wastes kinetic
energy, drastically increases traversal time, and induces significant mechanical fatigue on the transmission.

A truly optimized trajectory---analogous to a human driving a racing vehicle---anticipates corners, smooths the
transitional geometry, cuts aggressively across open spaces (such as $2 \times 2$ cell regions), and maintains the
maximum possible momentum bounded only by the physical friction limits of the tire contact patches. To realize this
behavior, a `TrajectoryOptimizer` must be constructed to execute three discrete, sequential transformations on the raw
orthogonal cell path: topological simplification through diagonal extraction, geometric smoothing via clothoid arc
injection, and dynamic motion profiling through S-curve velocity generation.

This report provides an exhaustive architectural and mathematical blueprint for constructing such an optimizer within a
`no_std` embedded Rust environment, specifically tailored for a four-wheeled skid-steer platform. This configuration
introduces severe non-holonomic complexities and non-linear wheel slip dynamics that must be actively compensated for
via feedforward torque control. By systematically addressing these domains, the control software can transform the
robotic agent from a reactive grid-follower into a highly anticipatory, kinetically optimized system.

Topological Path Simplification and Diagonalization
---------------------------------------------------

Before generating spatially smooth curves, the raw array of orthogonal Cartesian coordinates produced by a maze-solving
algorithm---such as Flood-fill, Dijkstra's, or A*---must be semantically simplified. An optimal trajectory does not
strictly adhere to the Manhattan distance of the labyrinth grid. Instead, it utilizes diagonal channels to minimize the
total path distance and reduce the cumulative angular velocity required to traverse complex sections.

### Orthogonal to Diagonal String Translation

The initial phase of optimization requires parsing the cell-to-cell coordinate sequence into a sequence of elementary
orthogonal actions. Rather than treating the path as a collection of absolute $(X, Y)$ coordinates, the optimizer
translates the path into a relative instruction string (e.g., Forward, Left, Right, Stop). Once this foundational string
is generated, a pattern-matching state machine applies heuristic reduction rules.

For a competitive skid-steer robot, running along diagonals avoids the severe deceleration penalties associated with
repetitive orthogonal 90-degree turns. The state machine evaluates look-ahead sequences within the instruction string to
identify opportunities for diagonal insertion. The transition from an orthogonal state to a diagonal state, and vice
versa, relies on a defined set of transitional primitives that map strictly to geometric parameters.

| **Command Primitive** | **Description**           | **Kinematic Implication**                                                                                        |
|-----------------------|---------------------------|------------------------------------------------------------------------------------------------------------------|
| **SS90**              | Straight-to-Straight 90°  | Standard orthogonal cornering. Requires moderate deceleration and significant lateral friction overhead.         |
| **SD45**              | Straight-to-Diagonal 45°  | Entry into a diagonal channel. Requires half the angular displacement of SS90, allowing higher entry velocities. |
| **DD90**              | Diagonal-to-Diagonal 90°  | An orthogonal turn executed while traversing a diagonal path, effectively navigating a zig-zag corridor.         |
| **DS45**              | Diagonal-to-Straight 45°  | Exit from a diagonal channel back to an orthogonal grid alignment.                                               |
| **SS180**             | Straight-to-Straight 180° | U-turn maneuver. Demands the highest angular acceleration and lowest linear speed to maintain traction.          |

The state machine scans the orthogonal string for specific repeating patterns. For example, the sequential string "
Left-Right-Right-Left" (`LRRL`) detected during orthogonal traversal indicates a zig-zag path. From a human perspective,
maintaining an orthogonal heading through a zig-zag is highly inefficient. The state machine mathematically reduces this
sequence to a single Diagonal-to-Diagonal 90-degree turn (`DD90R`) combined with diagonal straight segments. By
replacing these discrete orthogonal steps with diagonal primitives, the physical distance of the path is shortened, and
the temporal cost is mathematically reduced. The traversal width of a diagonal passage is approximately $114$
millimeters, which requires higher sensor precision to navigate but rewards the system with up to a $30\%$ reduction in
traversal time.

### The Vacuum Effect and Line-of-Sight Heuristics

A critical hallmark of "human-like" path planning is the ability to exploit open spaces, a phenomenon commonly referred
to in autonomous navigation heuristics as the "Vacuum Effect". When the robot enters an open region
spanning $2 \times 2$ cells or larger---where interior walls are absent---enforcing grid-aligned or even standard
diagonal $45$-degree movement remains inefficient. A human driver would naturally take the most direct racing line
through the open space.

The optimizer must identify these open regions by cross-referencing the proposed path with the stored wall-map of the
labyrinth. When a sequence of moves traverses an open $2 \times 2$ block, the optimizer replaces the multi-segment
boundary path with a single "Line of Sight" diagonal trajectory connecting the entry node directly to the exit node.

The geometric advantage of this optimization is substantial. Navigating the perimeter of a $2 \times 2$ cell block
orthogonally requires traveling a distance of $2 \cdot LAB\_CELL$. By substituting this with a line-of-sight hypotenuse
trajectory, the traversal distance becomes $\sqrt{2} \cdot LAB\_CELL$. This yields a direct spatial reduction of
approximately $29.3\%$. Furthermore, because the robot executes a single, shallow turn to enter the hypotenuse rather
than multiple orthogonal maneuvers, the dynamic velocity profile can maintain a substantially higher continuous peak
speed. The trajectory state machine must be augmented to recognize these bounded open areas and project virtual
straight-line segments that bypass the cellular center-points entirely.

Geometric Smoothing via Clothoid Transitions
--------------------------------------------

Once the path is topologically reduced to a sequence of straight segments and turn primitives (e.g., `SS90`, `SD45`),
the optimizer must translate these logical primitives into drivable geometric curves. The baseline model utilizes
`Segment::TurnInPlace`, necessitating a full halt. To maintain kinetic energy, turns must be executed as smooth spatial
arcs that blend seamlessly into the straight segments.

### The Geometric Limitations of Circular Arcs

A mathematically naive approach to cornering involves inserting a simple circular arc with a fixed radius $R$ at the
geometric intersection of two straight lines. While this methodology ensures spatial continuity ($G^0$) and tangent
continuity ($G^1$), it fundamentally violates curvature continuity ($G^2$).

On a straight line segment, the spatial curvature $\kappa$ is exactly $0$. On a circular arc, the curvature is a
constant $\kappa = 1/R$. If a robotic vehicle transitions instantaneously from a straight line to a circular arc, the
system demands an instantaneous, infinite step-change in angular acceleration---a mechanical impossibility. Attempting
to track such a path forces the control loop to induce massive torque spikes. In a skid-steer vehicle, this causes the
wheels to break static friction and slip unpredictably, leading to catastrophic accumulation of odometry errors and
subsequent wall collisions.

### Implementing Euler Spirals (Clothoids)

To achieve genuine "human-like" cornering that respects the laws of classical mechanics, the optimizer must utilize an
Euler Spiral, commonly known as a Clothoid. A clothoid is a specialized parametric curve whose curvature $\kappa(s)$
changes linearly with its arc length $s$. This linear change in spatial curvature corresponds directly to a linear
change in angular velocity, which inherently implies a constant, bounded angular acceleration.

The clothoid is mathematically defined by the Fresnel integrals, which provide the exact Cartesian coordinates $(x, y)$
as a function of the curve parameter $u$:

$$x(s) = \int_0^s \cos\left(\frac{\pi u^2}{2}\right) du$$

$$y(s) = \int_0^s \sin\left(\frac{\pi u^2}{2}\right) du$$

For practical robotic trajectory generation, a complete smooth turn is constructed using three distinct, symmetric
geometric phases to ensure continuous curvature from entry to exit :

| **Turn Phase**                | **Geometric Profile** | **Kinematic Behavior**                                                                                                                                 |
|-------------------------------|-----------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Phase 1: Entry Transition** | Clothoid Arc          | Transitions smoothly from $\kappa = 0$ to $\kappa = 1/R$. Angular velocity ramps up linearly based on a defined angular acceleration bound ($\alpha$). |
| **Phase 2: Apex**             | Circular Arc          | Constant curvature $\kappa = 1/R$. Angular velocity ($\omega$) is constant. Centripetal acceleration is at its absolute peak for the maneuver.         |
| **Phase 3: Exit Transition**  | Clothoid Arc          | Transitions smoothly from $\kappa = 1/R$ back to $\kappa = 0$. Angular velocity ramps down linearly to zero as the robot straightens out.              |

### Parameterizing the Turn in Embedded Systems

In a `no_std` embedded Rust implementation, dynamically computing Fresnel integrals in real-time using numerical
integration on a constrained microcontroller is computationally prohibitive. Instead, the `TrajectoryOptimizer` must
utilize a deterministic parametrization approach that approximates the clothoid using a trapezoidal angular velocity
profile.

The trajectory generator requires four specific tuning parameters to constrain a specific corner primitive :

1. **Target Velocity ($v$)**: The linear speed at which the turn will be executed.

2. **Arc Radius ($R$)**: The minimum turning radius achieved during the Phase 2 apex.

3. **Delta ($d$)**: The longitudinal distance over which the entry clothoid (Phase 1) occurs.

4. **Offset**: The physical setback distance from the geometric intersection of the labyrinth cells where the turn must
   initiate.

Using these inputs, the trajectory generator calculates the necessary peak angular velocity ($\omega = v / R$) and the
constant angular acceleration required to reach it ($\alpha = \omega / (d / v)$). The greater the linear velocity, the
larger the required turning radius, and the greater the necessary offset distance. If the calculated offset exceeds the
physical dimensions of the incoming maze cell, the turn is geometrically impossible at that speed. In such cases, the
velocity profile must enforce a deceleration zone before the turn begins, dynamically shifting the transition points. By
pre-calculating these clothoid parameters during the optimization phase, the robot transforms discrete cell
intersections into fluid, continuous-curvature racing lines.

Kinematic Modeling of Four-Wheel Skid-Steering
----------------------------------------------

The physical architecture of a four-wheeled skid-steer Micromouse fundamentally complicates trajectory execution. Unlike
Ackermann steering or two-wheeled differential drives, a four-wheel skid-steer vehicle possesses no active steering
mechanism. It relies entirely on differential thrust between the left and right wheel banks to induce a yaw moment. This
mechanical paradigm is robust but kinematically hostile, as it inherently mandates lateral wheel slip to achieve any
rotation.

### The Instantaneous Center of Rotation (ICR) and Effective Wheelbase

When a skid-steer robot attempts to follow a smooth clothoid curve, all four wheels must drag laterally across the maze
floor. This dragging induces massive frictional resistance and non-linear tire shear. The ideal kinematic equations for
a standard differential drive assume a track width $B$ (the physical distance between the left and right wheels).
However, due to longitudinal slip and lateral friction, the geometric track width does not accurately represent the
vehicle's true turning dynamics.

The system must be mathematically modeled using an Effective Wheelbase ($B_{eff}$), determined by the Instantaneous
Centers of Rotation (ICR) of the wheel contact patches. The ICR coefficient $\chi$ is a terrain-dependent and
dynamics-dependent variable such that $B_{eff} = \chi \cdot B$, where $\chi \ge 1$. For clean rubber tires on a painted
wooden maze floor, $\chi$ can fluctuate unpredictably. If the control software uses ideal differential kinematics, the
robot will systematically under-turn.

The transformation from the trajectory's desired robot body velocities (linear velocity $v$, angular velocity $\omega$)
to the left and right wheel velocities ($v_L, v_R$) becomes dependent on this effective wheelbase:

$$v_L = v - \frac{\omega \cdot B_{eff}}{2}$$

$$v_R = v + \frac{\omega \cdot B_{eff}}{2}$$

To model the slip, the longitudinal sliding friction and lateral forces for each wheel (e.g., the front-right wheel)
must be accounted for. The shear deformation modulus $K_r$ and the normal pressure $p_r$ dictate the frictional force,
which can be modeled via the integration of the contact patch:

$$F_{xfr} = \int_{-L/2}^{L/2} \int_{-b/2}^{b/2} p_r \mu_r (1 - e^{-j_{fr}/K_r}) \sin(\pi + \gamma_{fr}) dx_r dy_r$$

Where $j_{fr}$ is the shear displacement and $\gamma_{fr}$ is the sliding velocity angle. While solving this integral in
real-time is impossible for a microcontroller, the foundational takeaway is that high-speed cornering requires
drastically more torque than straight-line acceleration to overcome $F_{xfr}$.

### Dynamic Feedforward Torque Compensation

In the baseline `Trajectory.execute()` block of the user's code, positions are sent via a channel to a motor controller.
However, because of the massive frictional losses inherent to skid steering, relying purely on
Proportional-Integral-Derivative (PID) feedback control based on wheel encoder position will result in unacceptable
tracking errors during clothoid transitions. As the trajectory commands an angular acceleration to enter Phase 1 of the
turn, a standard PID controller must accumulate a significant error before it commands enough torque to overcome the
static lateral friction holding the wheels in a straight line.

To achieve fluid motion, the trajectory optimizer must supply *feedforward* velocities and accelerations directly to the
lower-level motor controllers alongside the spatial coordinates. The required torque $\tau$ for each motor bank is a
function of the vehicle's inertia $I_z$, mass $m$, and the calculated lateral friction. The control signal must be
augmented by feedforward constants ($K_{ff}$) that anticipate the exact torque required to force the skid.

$$\tau_{cmd} = \tau_{PID} + K_{ff\_vel} \cdot \omega_{target} + K_{ff\_accel} \cdot \alpha_{target} + \tau_{skid\_loss}$$

By executing the exact clothoid geometry in the trajectory optimizer, the instantaneous angular
acceleration $\alpha_{target}$ is known deterministically ahead of time. This value is passed down the `PATH_CHANNEL`,
allowing the motor driver to apply an immediate torque spike precisely at the initiation of the turn, completely
eliminating tracking lag and ensuring the robot perfectly traces the intended $G^2$ continuous curve.

Jerk-Limited Velocity Profiling and S-Curves
--------------------------------------------

The geometry of the path dictates *where* the robot travels, but the velocity profile dictates *when* and *how fast* it
moves along that path. The baseline implementation operates at a static, unoptimized speed (`speed: 0.5`). To compete at
elite levels, the robotic agent must aggressively accelerate on long straightaways and decelerate smoothly when entering
corners, pushing the physical limits of traction.

### The Limitations of Trapezoidal Velocity Profiles

The industry standard for basic robotic motion control is the trapezoidal velocity profile. In a trapezoidal profile,
the robot accelerates at a constant maximum rate until it reaches its target cruising speed, maintains that speed, and
then decelerates at a constant maximum rate.

While computationally cheap and easy to implement using simple Newtonian kinematics, trapezoidal profiles are inherently
flawed for high-performance skid-steer systems. The profile requires an instantaneous step-change from zero acceleration
to maximum acceleration. This infinite rate of change of acceleration is mathematically defined as *jerk* ($J = da/dt$).

An infinite jerk command acts as a violent step-input disturbance to the mechanical chassis. In a lightweight
Micromouse, this sudden jerk compresses the tire sidewalls, introduces suspension bounce, and excites resonant
frequencies within the motor gearboxes, resulting in microscopic wheel slip. When the wheels slip longitudinally, the
internal odometry is corrupted. Over a long run, this slip accumulates, causing the robot's internal Cartesian map to
diverge from reality, inevitably leading to a wall collision.

### The 7-Phase S-Curve Kinematic Model

To achieve optimized, human-like acceleration and deceleration, the trajectory optimizer must implement a jerk-limited
motion profile, universally known as an S-Curve. By strictly bounding the maximum allowable jerk, the acceleration ramps
up gradually. This creates a smooth, "S"-shaped velocity curve that prevents tire slip, preserves odometry, and
maintains total chassis stability.

An S-Curve motion profile is mathematically divided into seven distinct temporal phases :

| **S-Curve Phase**             | **Kinematic State**      | **Jerk (J)** | **Acceleration (a)**   | **Velocity (v)**     |
|-------------------------------|--------------------------|--------------|------------------------|----------------------|
| **1: Acceleration Build-up**  | Ramping up acceleration  | $+J_{max}$   | Linear increase        | Cubic curve          |
| **2: Constant Acceleration**  | Maximum acceleration     | $0$          | $+a_{max}$             | Parabolic curve      |
| **3: Acceleration Ramp-down** | Easing into cruise speed | $-J_{max}$   | Linear decrease        | Cubic curve          |
| **4: Constant Velocity**      | Cruising at peak speed   | $0$          | $0$                    | Constant ($v_{max}$) |
| **5: Deceleration Build-up**  | Easing into braking      | $-J_{max}$   | Linear negative        | Cubic curve          |
| **6: Constant Deceleration**  | Maximum braking          | $0$          | $-a_{max}$             | Parabolic curve      |
| **7: Deceleration Ramp-down** | Easing to target speed   | $+J_{max}$   | Linear increase to $0$ | Cubic curve          |

By adhering to this 7-phase model, the velocity equation $v(t)$ evolves smoothly over time, preventing any instantaneous
spikes in required motor torque.

### Integrating Spatial Constraints with Velocity Profiling

The true complexity of the `TrajectoryOptimizer` arises when mapping this 1D time-based S-Curve profile onto the 2D
spatial geometry.

The optimizer must perform a backward-pass over the entire simplified geometric path. It must analyze every smooth
clothoid corner to determine its maximum safe traversing speed based on centripetal acceleration
limits ($a_{lat\_max}$). The velocity at the apex of the corner must be constrained such
that $v_{corner} \le \sqrt{a_{lat\_max} \cdot R}$.

Once the target entry velocity for every corner is established, the optimizer calculates the S-curve deceleration
required to safely enter the corner from the preceding straightaway. If the straight segment is too short to allow the
robot to reach maximum physical speed and still decelerate in time, the S-curve is dynamically truncated. In these
scenarios, Phase 4 (Constant Velocity) is eliminated, resulting in a triangular or partially-formed S-profile.

This demonstrates the extreme value of coupling the $2 \times 2$ long-diagonal path simplification with S-curve
generation. Because a line-of-sight hypotenuse through an open space yields a substantially longer straight-line
segment, it provides the requisite spatial and temporal runway for the S-curve generator to execute all 7 phases,
reaching much higher peak velocities (Phase 4) before deceleration becomes necessary.

Embedded Rust Architecture and `no_std` Integration
---------------------------------------------------

Implementing this sophisticated, mathematically intensive trajectory optimization within the strict constraints of a
`no_std` Rust environment requires exceptionally careful memory management, type system design, and algorithmic
selection. The optimizer must process the raw map without dynamically allocating large heaps, and it must execute
complex trigonometric functions without access to the Rust standard library.

### Rethinking Traits and Data Structures

To facilitate advanced profiling, the baseline system must be completely overhauled. The original `TrajectoryOptimizer`
trait and `Segment` enum lack the parameterization necessary for feedforward execution.

Rust

```
pub trait TrajectoryOptimizer {
    fn optimize(&self, raw_trajectory: Trajectory, lab: &Labyrinth) -> OptimizedTrajectory;
}

```

The output of the optimizer is an `OptimizedTrajectory` comprising a new set of continuous, mathematically bound
segments:

Rust

```
#
pub enum OptimizedSegment {
    /// S-curve controlled straight line
    Straight {
        distance: f32,
        entry_v: f32,
        exit_v: f32,
        max_v: f32
    },
    /// Clothoid-Arc-Clothoid combination
    SmoothTurn {
        angle: f32,
        radius: f32,
        delta: f32,
        apex_velocity: f32,
        direction: TurnDirection
    },
}

```

Because the `alloc` crate is often restricted or avoided in strict bare-metal environments to prevent heap
fragmentation, out-of-memory panics, and deterministic execution failures , the optimized trajectory must be stored
using fixed-capacity stack-allocated vectors. Ecosystem crates such as `heapless` or `arrayvec` are uniquely suited for
this purpose. The maximum number of motion segments is deterministically bounded by the maximum possible path length in
a $16 \times 16$ dimension labyrinth, ensuring that fixed-size arrays can be safely allocated at compile time without
overflow.

### Mathematical Approximations in `no_std`

S-Curve generation and Clothoid transitions require heavy use of square roots, sines, cosines, and arc-tangents. In
`no_std` environments, standard `f32::sin()` and `f32::sqrt()` provided by the `std` library are completely unavailable.
The provided `micromath::F32Ext` trait is indispensable in this context.

`micromath` leverages rapid polynomial approximations (such as Taylor series expansions) and Newton-Raphson iterations
to provide trigonometric functions that execute in deterministic time with exceptionally small code footprints. This is
achieved at an acceptable trade-off of approximately $0.002$ radians of precision loss. For trajectory execution, this
microscopic loss of precision is easily mitigated by the robot's lateral closed-loop wall-tracking infrared sensors.
However, to prevent integration drift over long open diagonals (where side walls are out of range and sensors cannot
correct the heading), the `OptimizedSegment::Straight` calculations must be continually evaluated against internal
gyroscope data to ensure the `micromath` drift does not compound beyond the physical tolerances of the maze cell.

### Leveraging Jerk-Limited S-Curve Crates

Instead of manually deriving the complex seven-phase piecewise integration for every movement, the Rust ecosystem
provides specialized `no_std` motion profiling crates such as `scurve_motion` and `s_curve` that can be directly
integrated into the optimization pipeline.

These crates accept constraints defined purely in SI units:

- `max_jerk` ($J_{max}$ in $m/s^3$)

- `max_acceleration` ($a_{max}$ in $m/s^2$)

- `max_velocity` ($v_{max}$ in $m/s$)

- Boundary conditions for entry and exit velocities ($v_0, v_1$)

The S-curve generators process these inputs and output a struct containing parametric mapping functions. This allows the
real-time execution loop to query the exact velocity and acceleration for any given timestamp $t$, fundamentally
decoupling the heavy optimization math from the high-frequency control loop.

### Asynchronous Execution and Channel Communication

The final execution pipeline bridges the gap between the statically computed geometric trajectory and the real-time
kinematic motor controller. In the user's original implementation, the `execute()` block sent raw $X, Y, \theta$
coordinates across the `PATH_CHANNEL` at fixed time steps `DT`. This naive implementation assumed constant speed and
instantaneous direction changes, which destroys the utility of an optimized path.

For the advanced optimizer, the execution loop must integrate the outputs of the S-curve generator and the Clothoid
equations simultaneously, transmitting feedforward data to the hardware.

1. An asynchronous loop runs at the fundamental discrete time step `DT`.

2. At each step, the S-Curve equation is queried for the current elapsed time $t$, yielding the instantaneous required
   linear velocity $v(t)$ and linear acceleration $a(t)$.

3. The geometric segment (Straight or SmoothTurn) is queried to determine the current spatial curvature $\kappa(s)$ at
   the integrated distance $s$.

4. The kinematics engine mathematically merges these properties to determine instantaneous angular
   parameters: $\omega(t) = v(t) \cdot \kappa(s)$, and calculates the corresponding angular acceleration $\alpha(t)$
   based on the clothoid derivative.

5. These values---$x, y, \theta, v, \omega, a, \alpha$---are packaged into an extended `PathPoint` struct and
   transmitted via `PATH_CHANNEL` to the motor drivers.

At the receiving end of the channel, the low-level interrupt routines driving the motors operate purely as tight,
high-frequency torque regulators. By receiving time-indexed velocity and acceleration targets directly from the channel,
the motor driver effortlessly applies the feedforward skid-steer torque calculations, forcing the chassis to perfectly
track the clothoid arcs without the processing overhead of generating the trajectory on the fly.

Conclusion
----------

Elevating a Micromouse trajectory from discrete, grid-bound movements to fluid, highly dynamic "human-like" motion
requires an exhaustive, interconnected approach that addresses topology, geometry, kinematics, and control theory
simultaneously. The baseline assumption that a robotic agent must travel orthogonally and halt at every corner
fundamentally cripples performance. The implementation of diagonal path parsing and $2 \times 2$ spatial optimization
through line-of-sight heuristics radically reduces the physical traversal distance and minimizes the accumulated
rotational burden imposed by the maze layout.

However, altering the structural topology of the path is entirely insufficient without rigorous geometric and dynamic
smoothing. Because the four-wheeled skid-steer architecture mandates high frictional wheel slip and lateral tire shear
to achieve rotation, the application of Clothoid Euler spirals is absolutely critical to avoid infinite jerk and
catastrophic traction loss during cornering. By pairing these continuous-curvature spatial arcs with a mathematically
rigorous 7-phase S-curve velocity profile, the system ensures that linear and angular acceleration vectors are applied
smoothly and predictably, preserving odometry integrity and ensuring total mechanical stability.

When meticulously implemented within a `no_std` embedded Rust environment---leveraging optimized mathematical
approximations, stack-allocated data structures, and deterministic asynchronous execution loops---this multi-layered
`TrajectoryOptimizer` yields a highly resilient, time-optimal solution. The fusion of predictive geometric modeling,
kinematic feedforward torque compensation, and jerk-limited velocity profiling completely transforms the robotic agent.
It ceases to be a simple, reactive mechanism blindly following a grid, and becomes a highly anticipatory, kinetically
optimized system capable of operating at the absolute maximum physical limits of traction and speed.